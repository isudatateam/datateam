dependencies:
  - coverage
  # help dep solver
  - gdal>=3.10,<3.11
  # legacy
  - google-api-python-client
  - google-auth
  # Help dep solver
  - libnetcdf=4.9.2
  - numpy
  # legacy
  - psycopg2
  # Ensure a bit of discipline
  # and help dep solver get it right
  - pyiem >=1.25
  # testing
  - pytest
  - pytest-cov
  # legacy stuff
  - smartsheet-python-sdk
  # for pandas
  - sqlalchemy
  # testing
  - werkzeug
